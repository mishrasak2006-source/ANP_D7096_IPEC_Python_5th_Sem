current_balance = float(input("Enter current balance: "))
withdrawal = float(input("Enter withdrawal amount: "))

remaining_balance = current_balance - withdrawal
print("Remaining Balance:", remaining_balance)
